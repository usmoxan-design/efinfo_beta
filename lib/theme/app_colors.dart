import 'package:flutter/material.dart';

class AppColors {
  // Asosiy fon rangi (Ultra to'q qora)
  static const Color background = Color(0xFF0A0A0A);

  // Sirtlar va kartalar uchun fon (Silliq to'q kulrang)
  static const Color surface = Color(0xFF161616);
  static const Color cardSurface = Color(0xFF1C1C1E);

  // Border rangi (Juda nozik)
  static const Color border = Color(0x1AFFFFFF);

  // Asosiy urg'u ranglari (Rasmga mos yorqin ranglar)
  static const Color accent = Color(0xFF06DF5D); // Asosiy (Pink)
  static const Color accentPink = Color(0xFF06DF5D); // Pink accent
  static const Color accentGreen = Color(0xFF00F294); // Neon Green
  static const Color accentOrange = Color(0xFFFFA500); // Vibrant Orange
  static const Color accentBlue = Color(0xFF00CFFF); // Bright Blue

  // Telegram rangi
  static const Color telegram = Color(0xFF229ED9);

  // Matnlar uchun
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGrey = Color(0xFFA0A0A0);
  static const Color textDim = Color(0xFF636366);

  // Xatolik rangi
  static const Color error = Color(0xFFFF453A);
}
