import 'package:flutter/material.dart';

class AppColors {
  // Asosiy fon rangi (To'q yashil/qora)
  static const Color background = Color(0xFF011A0B);

  // Asosiy urg'u rangi (Yorqin yashil)
  static const Color accent = Color(0xFF06DF5D);

  // Navigatsiya paneli va AppBar uchun fon (To'qroq kulrang)
  static const Color surface = Color(0xFF1A1A1A);

  // Ikkinchi darajali fon (masalan, panellar, kartochkalar uchun)
  static const Color cardSurface =
      Color(0xFF0D2418); // Backgrounddan ozgina farq qiladi

  // Telegram rangi
  static const Color telegram = Color(0xFF229ED9);

  // Oq matnlar uchun
  static const Color textWhite = Colors.white;
  static const Color textGrey = Colors.white70;

  // Xatolik rangi
  static const Color error = Colors.redAccent;
}
